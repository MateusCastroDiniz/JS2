// Acredito que terei que declarar os tipos de dados, como no Java

// tuple
let cores : [string, number]

cores = ['rosa', 1]

enum CoresDicio {
    vermelho ='vermelho',
    verde ='verde',
    amarelo = 'amarelo'
}